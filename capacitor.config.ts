import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'matrimony.akshaya.ionic',
  appName: 'Matrimony',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
